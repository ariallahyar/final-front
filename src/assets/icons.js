import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faLocationDot as address, faHome as homepage } from "@fortawesome/free-solid-svg-icons";

export const location = <FontAwesomeIcon icon={address} />;
export const website = <FontAwesomeIcon icon={homepage} />;
